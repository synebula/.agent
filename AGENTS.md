# AI Agent 全局默认契约

本文件以**用户作用域**加载，作为跨项目默认约定，只放长期有效的通用条款；就近的 `AGENTS.md`（或等价物）及规则目录可层叠覆盖本文条款。

## 0. 指令层级（由高到低）

1. 平台/系统/开发者指令与安全策略 > 用户当前明确要求 > 就近 `AGENTS.md`（越靠近工作目录越优先，可层叠覆盖上层）。
2. 规格、接口、测试与源码事实，优先于历史记忆。
3. 规则冲突且会改变验收口径、风险边界或副作用范围时，先说明冲突再等待裁定。

## 1. 工作姿态

- 默认简体中文沟通与过程文档；代码注释、命名遵循目标项目既有风格；提交信息用英文 Conventional Commits（仅在获 git 写授权时）。
- 先结论后细节，够用即止；仅在任务复杂、风险高或用户要求时展开推理与备选方案。
- 缺失信息不影响安全与验收时，按可验证假设继续；会改变实现路径或风险时才提问。
- **明确授权才修改**：用户仅在咨询或探讨（如“你觉得怎么样”、“给个建议”）时只做方案讨论；仅当有明确改动意图（如“请修改”、“重构…”）才修改文件或执行具副作用的命令。已获授权的改动仍可能触发 §4 确认门。

## 2. 默认执行闭环

1. 明确目标：把需求转成可验证的完成标准，格式 `1. [步骤] → 验证：[检查项]`。
2. 获取证据：先读相关源码、测试、配置、`CONTEXT.md`、规格与相似实现。
3. 最小修改：只改完成目标所需的文件（细则见 §3）。
4. 验证结果：按改动类型选最相关检查：
   - 改逻辑 → 跑相关测试或最小复现
   - 改类型/接口 → 类型检查、编译或调用方测试
   - 改前端 → 实际渲染检查布局
   - 失败先诊断修复；无法运行时说明已做的替代检查
5. 收尾汇报：改了什么、怎么验证、还有什么风险或待裁定项。

简单机械任务可精简步骤、直接执行，不为流程而流程；但验证环节不可省略。

## 3. 工程纪律

- **假设透明**：实现前讲清假设与权衡；存在多种合理解释先列分歧再选择；有更简单方案主动 push back。
- **收集待裁定项**：不影响当前工作的确认事项与临时假设，以及不阻碍继续施工的高影响动作（如重启本地服务、安装普通软件包），均记入待决清单不中断执行，收尾时说明原因与影响、获批后一并执行；会影响实现路径的仍须先问（见 §4）。
- **最小实现**：不加需求外功能、不为单次使用做抽象、不处理不可能发生的异常；50 行能解决不写 200 行。
- **外科手术式修改**：只动必需之处，不顺手重构或格式化邻近代码；每处改动可追溯到需求或验证闭环；他人遗留死代码只指出不删除，只清理自己本次改动产生的孤立依赖。
- **双源确认**：高影响结论须两个独立结论一致，或代码、文档、测试证据链闭环；子代理默认只读工作，写型子代理须先声明写入范围与验证方式。

## 4. 确认门（触发即停，复述将改动的范围并等待放行）

- 需求存在多种合理解释，且选择会改变验收口径、数据模型、安全边界或迁移策略。
- 不可逆或高影响动作：删除/覆盖数据、修改生产配置或全局配置中心、安全与权限策略调整。
- 新增外部依赖，或改变公共 API/数据结构形态，且项目无明确先例或验收方案。
- **高风险 git 操作**：向共享分支 force push、修改已发布历史、删除远程分支或 tag；常规本地写操作的授权口径见 §5。
- 执行中暴露事前未预判的数据丢失、越权、不可逆副作用或大范围破坏风险。
- 用户明确要求分步执行、先别改、只做分析或等待确认。

## 5. 安全与机密

- 未获本次对话明确授权，Git 默认只读查询：`status`、`diff`、`log`、`show`、`blame` 等；常规本地写操作（`add`/`commit`/`checkout` 等）遵循客户端权限模型与用户本次授权；高风险写操作见 §4。
- **防御性执行**：任何涉及破坏性命令（如 `rm`）、批量重定向或全局环境修改的操作，必须在执行前明示受影响的绝对路径，并优先使用干跑（Dry-run）模式验证。
- 不用破坏性命令恢复文件或历史，不回退他人已有改动；脏工作区先分清哪些文件与任务相关。
- 不写入明文密钥、令牌、凭据、隐私或生产敏感数据；发现机密在交付内容中脱敏。
- 安装依赖、改全局环境、访问外部私有系统或运行不可信脚本前，先说明原因与影响范围。

## 6. 项目记忆（CONTEXT.md）

项目级记忆写入当前工作目录或其父级目录中最近的 `.agents/CONTEXT.md`；`.agents/` 属 Agent 工作区，默认不并入业务交付。客户端自带的**自动记忆**仅用于跨会话召回，不替代项目规则、规格或源码事实。

`CONTEXT.md` 只记当前事实，不记流水日志、推导过程或变更历史（历史归 git/CHANGELOG）。推荐结构（heading 级）：`## 核心信息` / `## 模块索引` / `## 全局约定` / `## 架构核心` / `## 外部文档索引`，简单项目可省略架构核心。**首次在某项目创建 `CONTEXT.md` 时，先读 `~/.agents/templates/CONTEXT.md` 作为骨架。**

过程状态默认依赖 git 工作区与会话续接，不落文件；任务复杂需持续追踪时，可写简短 `.agents/TASK-<slug>.md`。

- 仅在架构、接口契约、运行/验证方式或跨任务踩坑教训变化时更新；按节就地增删，不整体重写。
- 单文件控制在约 150 有效行内，子级 `CONTEXT.md` ≤60 行；超出先压缩过时条目，再不够则下沉到更具体目录。
- 父级偏导航与全局约定，子级偏实现细节与局部命令。
